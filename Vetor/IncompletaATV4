/*Escreva um programa que alimente um vetor A com 5 números inteiros. 
Em um vetor B, armazene odobro de cada um dos números vetor A e apresente o vetor B na tela.*/

#include <stdio.h>

int main(){
	
	int A[5], B[5], i, j;
	
	
	for(i = 0; i < 5; i++){
		printf("Informe numero: ");
		scanf("%d", &A[i]);
		
	for(j = 0; j < 5; j++){
		B[j] = A[i] * 2;
	}
	
	}
	
	for(j = 0; j < 5; j++){
		printf("\nNumeros informados: %d", B[j]);
	}
	
	
	
	return 0;
}
