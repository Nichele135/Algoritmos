/*Escreva um programa que mostre o tamanho de uma string informada pelo usuário (utilize a funçãoapropriada).*/

#include <stdio.h>
#include <string.h>

int main (){
	
	char str[16];
	
	printf("Informe seu nome: ");
	scanf(" %20[^\n]", str);
	
	printf("Caracteres: %d", strlen(str));
	
	return 0;
}
